generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

enum Role {
  STUDENT
  COMPANY
  ADMIN
  REDACTOR
}

model User {
  id            String   @id @default(cuid())
  name          String?
  email         String   @unique
  emailVerified DateTime?
  image         String?
  password      String?  // null for OAuth users
  role          Role     @default(STUDENT)
  bio           String?
  avatar        String?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  // Relations
  accounts     Account[]
  sessions     Session[]
  studentProfile StudentProfile?
  projects     Project[]
  forumPosts   ForumPost[]
  comments     ForumComment[]
  proposals    Proposal[]
  contactRequests ContactRequest[]
  feedbacks       Feedback[]
  newsletterEditions NewsletterEdition[]
}

// ------------------------------------------------------------------
// El Semanal — newsletter de la asociación
// ------------------------------------------------------------------

model NewsletterEdition {
  id        String    @id @default(cuid())
  number    Int       @unique // edición #1, #2...
  title     String // titular (IA + retoque del redactor)
  summary   String // resumen que va en el email
  pdfUrl    String // ruta del PDF alojado
  coverUrl  String? // portada opcional
  sentAt    DateTime? // null = borrador sin enviar
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  createdBy String
  author    User      @relation(fields: [createdBy], references: [id])
}

model NewsletterSubscriber {
  id        String   @id @default(cuid())
  email     String   @unique
  active    Boolean  @default(true) // false = dado de baja
  token     String   @unique @default(cuid()) // para link de baja
  source    String   @default("public") // public | member
  createdAt DateTime @default(now())
}

// Solicitud de alta de profesionales/profesores. NO crean cuenta directamente:
// contactan primero y la asociación les crea un correo institucional personalizado.
model JoinRequest {
  id        String   @id @default(cuid())
  name      String
  email     String
  type      String // "professional" | "professor"
  message   String?
  handled   Boolean  @default(false) // la asociación ya lo gestionó
  createdAt DateTime @default(now())
}

model StudentProfile {
  id        String   @id @default(cuid())
  userId    String   @unique
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  portfolioUrl String?
  skills    String[] // e.g., ["UX Design", "Graphic Design", "Branding"]
  interests String?
  course    String?  // e.g., "3rd Year Design"
  linkedin  String?
  website   String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Project {
  id          String   @id @default(cuid())
  userId      String
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  title       String
  description String   @db.Text
  images      String[] // URLs of uploaded images
  tags        String[] // e.g., ["branding", "ux", "web"]
  category    String?  // e.g., "Academic", "Personal", "Professional"
  visible     Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  feedbacks Feedback[]
}

model ForumPost {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  title     String
  content   String   @db.Text
  category  String?  // e.g., "Discussion", "Feedback", "Resources"
  pinned    Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  comments  ForumComment[]
}

model ForumComment {
  id        String   @id @default(cuid())
  postId    String
  post      ForumPost @relation(fields: [postId], references: [id], onDelete: Cascade)
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  content   String   @db.Text
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Proposal {
  id          String   @id @default(cuid())
  userId      String
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  title       String
  description String   @db.Text
  category    String?  // e.g., "Collaboration", "Project Idea", "Event"
  status      String   @default("open") // open, in_progress, completed, closed
  tags        String[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Company {
  id          String   @id @default(cuid())
  name        String
  email       String   @unique
  password    String
  description String   @db.Text
  website     String?
  logo        String?
  sector      String?  // e.g., "Tech", "Design", "Fashion"
  location    String?
  isVerified  Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  needs       CompanyNeed[]
  contactRequests ContactRequest[]
}

model CompanyNeed {
  id          String   @id @default(cuid())
  companyId   String
  company     Company  @relation(fields: [companyId], references: [id], onDelete: Cascade)
  title       String
  description String   @db.Text
  skills      String[] // what they're looking for
  status      String   @default("open") // open, interviewing, filled, closed
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model ContactRequest {
  id          String   @id @default(cuid())
  companyId   String
  company     Company  @relation(fields: [companyId], references: [id], onDelete: Cascade)
  studentId   String
  student     User     @relation(fields: [studentId], references: [id], onDelete: Cascade)
  message     String   @db.Text
  status      String   @default("pending") // pending, accepted, rejected
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model Feedback {
  id        String   @id @default(cuid())
  projectId String
  project   Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  rating    Int      // 1-5
  comment   String?  @db.Text
  createdAt DateTime @default(now())

  @@unique([projectId, userId]) // one feedback per user per project
}

// NextAuth models
model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}
